<Window x:Class="FontInstallerApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Font Installer" Height="400" Width="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBox x:Name="FolderPathText" Grid.Row="0" Grid.Column="0" Height="25" Margin="0,0,5,5" IsReadOnly="True"/>
        <Button Content="Browse..." Grid.Row="0" Grid.Column="1" Width="90" Click="BrowseButton_Click" Margin="0,0,0,5"/>

        <ListBox x:Name="LogBox" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,10,0,10" />

        <Button Content="Install Fonts" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Height="35" Click="InstallButton_Click"/>
    </Grid>
</Window>
